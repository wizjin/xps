language: c
os: osx
osx_image: xcode10
compiler: clang
sudo: false
before_install:
    - brew insatll check
script:
    - make test

after_success:
    - bash <(curl -s https://codecov.io/bash)

branches:
    only:
        - master
        - develop
